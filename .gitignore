# Site config and secrets are local-only
# Use *.example as starting templates
site.yaml
secrets.yaml
secrets.yaml.enc

# Site-specific configs (generated per-deployment via make host-config, make node-config)
hosts/*.yaml
nodes/*.yaml

# Runtime state (spec.yaml, config-complete.json)
.state/

# Editor files
*.swp
*~
.idea/
.vscode/

# OS files
.DS_Store
